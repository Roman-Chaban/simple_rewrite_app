import { type FC } from 'react';

import { type Metadata } from 'next';

export const metadata: Metadata = {
   title: 'CRM | Dashboard (InfoPortal)',
};

const InfoPortal: FC = () => {
   return <section>InfoPortal</section>;
};

export default InfoPortal;
