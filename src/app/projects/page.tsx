import { type FC } from 'react';

import { type Metadata } from 'next';

export const metadata: Metadata = {
   title: 'CRM | Dashboard (Projects)',
};

const Projects: FC = () => {
   return <section>Projects</section>;
};

export default Projects;
