import { type FC } from 'react';

import { type Metadata } from 'next';

export const metadata: Metadata = {
   title: 'CRM | Dashboard (Vacations)',
};

const Vacations: FC = () => {
   return <section>Vacations</section>;
};

export default Vacations;
