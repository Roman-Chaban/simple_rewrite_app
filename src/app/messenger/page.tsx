import { type FC } from 'react';

import { type Metadata } from 'next';

export const metadata: Metadata = {
   title: 'CRM | Dashboard (Messenger)',
};

const Messenger: FC = () => {
   return <section>Messenger</section>;
};

export default Messenger;
