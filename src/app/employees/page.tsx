import { type FC } from 'react';

import { type Metadata } from 'next';

export const metadata: Metadata = {
   title: 'CRM | Dashboard (Employees)',
};

const Employees: FC = () => {
   return <section>Employees</section>;
};

export default Employees;
